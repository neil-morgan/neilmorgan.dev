import { styled } from "@/lib/stitches";

export const IntroWrapper = styled("div", {
  display: "flex",
  background: "$primary2",
  color: "$primary1",
  padding: "$1 $3",
  borderRadius: "$md",
});
