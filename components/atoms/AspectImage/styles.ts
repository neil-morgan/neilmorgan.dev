"use client";

import { styled } from "@/lib/stitches";

export const AspectImageContainer = styled("div", {
  position: "relative",
  display: "flex",
  width: "100%",
  borderRadius: "$md",
  overflow: "hidden",
});
