query Skills($preview: Boolean = false) {
  skills: skillCollection(preview: $preview) {
    items {
      title
      slug
      proficiency
      icon {
        url
        title
      }
      tagsCollection {
        items {
          title
          slug
        }
      }
    }
  }
}
