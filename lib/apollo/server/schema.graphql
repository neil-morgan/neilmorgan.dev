type Post {
  _id: ID
  likes: Int
}

type Query {
  posts: [Post]
  post(_id: ID): Post
}

type Mutation {
  createPost(_id: ID, likes: Int): Post
  updatePost(_id: ID, likes: Int): Post
  deletePost(_id: ID): Post
}
