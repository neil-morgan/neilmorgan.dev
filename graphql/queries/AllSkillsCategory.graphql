query AllSkillsCategory($slug: String!) {
  skills: skillCollection(where: { category: { slug_contains: $slug } }) {
    items {
      title
      proficiency
      category {
        ...SkillCategoryFragment
      }
    }
  }
}
