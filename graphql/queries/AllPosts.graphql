query AllPosts {
  posts: postCollection(order: date_DESC) {
    items {
      sys {
        id
      }
      title
      description
      date
      slug
      tagsCollection {
        items {
          title
          slug
        }
      }
      category {
        ...PostCategory
      }
    }
  }
}
