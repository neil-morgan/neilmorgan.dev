query AllPosts {
  posts: postCollection(order: date_DESC) {
    items {
      sys {
        id
      }
      title
      description
      date
      slug
      category {
        ...PostCategoryFragment
      }
    }
  }
}
