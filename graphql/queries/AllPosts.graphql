query AllPosts {
  postCollection(order: date_DESC, limit: 10) {
    items {
      sys {
        id
      }
      title
      description
      date
      slug
      category {
        ...PostCategoryContentFragment
      }
    }
  }
}
