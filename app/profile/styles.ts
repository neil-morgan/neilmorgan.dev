"use client";

import { styled } from "@/lib/stitches";

export const PageWrapper = styled("div", {
  position: "relative",
  display: "flex",
  flexDirection: "column",
  minHeight: "100vh",
  margin: "0 auto",
});
