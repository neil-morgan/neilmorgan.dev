query HeaderContent {
  social: linkCollection(where: { label_in: ["LinkedIn", "Github"] }) {
    items {
      ...Link
    }
  }
  categories: pageCollection(where: { category_exists: true }) {
    items {
      ...Category
    }
  }
  uniquePages: pageCollection(
    where: { category_exists: false, slug_not: "/" }
  ) {
    items {
      ...PageMeta
    }
  }
}
